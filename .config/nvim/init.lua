require('colors').setup() -- my colors my style

require('plugins') -- all about plugins
require('mappings') -- custom mappings
require('settings') -- settings from vim
require('welcome') -- startify

require('completion').setup() -- setup completion
